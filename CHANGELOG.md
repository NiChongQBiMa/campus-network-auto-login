# 校园网自动登录 - 迭代记录

> 项目起始日期：2026-05-19

---

## v1.0 — 项目初始化

**目标：** 在 Windows 7 校园电脑上实现开机自动登录校园网认证系统。

### 需求调研
- 场景：学校 Win7 电脑，每天开机后需手动访问 `10.26.13.2` 输入账号密码
- 要求：开机自启、后台运行、账号密码存本地 INI 文件、登录结果弹窗提示
- 认证方式：网页表单（两个文本框 + 保存密码复选框 + 登录按钮）

### 方案对比
| 方案 | 结论 |
|------|------|
| Python + HTTP POST | ✅ 采用，轻量稳定，可打包为独立 exe |
| PowerShell 脚本 | ❌ Win7 自带 PS 2.0 功能太弱 |
| AutoHotkey + 模拟浏览器 | ❌ 定位网页元素不稳定，受分辨率影响 |

### 产出
- 设计文档 `docs/superpowers/specs/2026-05-19-校园网自动登录-design.md`
- 实现计划 `docs/superpowers/plans/2026-05-19-校园网自动登录-plan.md`
- 项目骨架：`README.md`、`.gitignore`、`config.example.ini`、`使用说明.txt`
- 核心代码 `src/main.py`（配置读写、三层网络检测、自适应表单分析、弹窗提示、日志）

---

## v1.1 — Win7 兼容打包

**问题：** 开发环境 Python 3.14 打包的 exe 依赖 `python314.dll`，不支持 Windows 7。

**解决：**
- 安装 Python 3.8.10（最后一个支持 Win7 的版本）
- 用 Python 3.8 的 PyInstaller 重新打包
- exe 大小从 15.51 MB 降至 9.48 MB（Python 3.8 标准库更精简）

---

## v1.2 — 浏览器 UA 伪装 + 智能重试

**问题：** 用户用外网网站测试时遇到 `RemoteDisconnected` 错误，服务器直接掐断连接。

**根因：** `main()` 中首次 GET 请求使用 Python 默认 UA `python-requests/2.x`，被反爬虫机制拒绝。

**修复：**
- 新增 `create_session()` 函数，统一伪装 Chrome 120 浏览器 UA
- 将 `perform_login()` 中硬编码的旧 UA 替换为 `create_session()`
- 集成 `urllib3.Retry`，网络波动时自动重试 3 次（指数退避）
- 错误提示分类：SSL 错误、连接拒绝、超时各给出明确建议
- 新增诊断日志：记录 HTTP 状态码、响应长度、异常短响应时输出片段

---

## v1.3 — 无 `<form>` 标签页面支持

**问题：** 用户用路由器管理页面测试，日志显示"页面中未找到登录表单"。该页面使用 JavaScript 处理登录，HTML 中没有 `<form>` 标签。

**修复：**
- `analyze_login_form()` 不再要求必须有 `<form>` 标签
- 无 `<form>` 时全局搜索 `<input>` 元素
- 智能匹配增强：同时检查 `name`、`id`、`placeholder` 属性
- 用户名模式扩展：`username`、`userid`、`account`、`uname`、`logname` 等
- 兜底策略：如果只有一个 text + 一个 password，自动配对
- 失败时输出页面上所有 input 元素列表，便于手动排查

---

## v1.4 — 环境迁移自动重新分析

**问题：** 程序缓存了表单结构到 `config.ini`，如果换到不同环境（如从路由器换到校园网），缓存的字段名不匹配会导致登录失败，但程序不会自动重新适应。

**修复：**
- 登录失败时自动清除 `[form]` 缓存
- 重新 GET 页面 → 重新分析表单 → 重新写入配置 → 重试登录
- 最多尝试 2 次（首次用缓存 + 1 次重新分析）
- 网络层错误（SSL/连接/超时）直接退出，不做无意义重试

---

## v1.5 — 部署脚本

**产出：**
- `设置开机自启.bat`：双击自动在启动文件夹创建快捷方式，无需手动操作
- 桌面部署测试包 `校园网登录-部署测试`：包含 exe、配置模板、使用说明、设置脚本

---

## 当前状态

| 项目 | 状态 |
|------|:--:|
| 网络检测（IP → TCP → HTTP 三层） | ✅ |
| 自适应表单分析（有/无 `<form>` 均支持） | ✅ |
| 浏览器 UA 伪装 | ✅ |
| 自动重试（网络波动 + 表单重分析） | ✅ |
| Win7 兼容 exe 打包 | ✅ |
| 弹窗提示 + 日志记录 | ✅ |
| 一键设置开机自启 | ✅ |
| Git 版本管理 | ✅ |

## 待验证

- 在真实校园网环境 `10.26.13.2` 上运行测试
- 在不同校园网认证系统（深澜/锐捷/华为/Dr.COM）上验证兼容性
